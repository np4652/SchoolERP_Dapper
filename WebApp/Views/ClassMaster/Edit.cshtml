@model Entities.Models.ClassMaster
@{

}
<div class="row">
    <div class="col-md-4">
        <form action="/class/save">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input value="@Model.ClassId" name="@nameof(Model.ClassId)" class="form-control" type="hidden" />
            </div>
            <div class="form-group">
                <label asp-for="ClassName" class="control-label">@nameof(Model.ClassName)</label>
                <input value="@Model.ClassName" name="@nameof(Model.ClassName)" id="@nameof(Model.ClassName)" class="form-control"/>
                <span asp-validation-for="ClassName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <span class="btn btn-link" onclick="loadData()">Back to List</span>
</div>

@{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}
